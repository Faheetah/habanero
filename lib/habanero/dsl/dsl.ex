defmodule DSL do
  @moduledoc """
  Constructs a DSL and returns a mapping of instructions to execute
  """

  @doc "Parses DSL text and returns a nested struct of all instructions"
  def parse(text), do: text
end
